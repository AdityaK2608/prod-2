const STORAGE_KEY='test-engine-v1';
const exams={stet:{id:'stet',name:'Bihar STET Paper II — Computer Science',short:'STET CS',questions:150,minutes:150,marks:150,sections:[{id:'subject',name:'Computer Science',count:100},{id:'pedagogy',name:'Teaching & Eligibility',count:50}],negative:0,cutoff:'General 50% · Reserved 40%'},tre:{id:'tre',name:'BPSC TRE 4.0 — Senior Secondary CS',short:'TRE 4.0 CS',questions:150,minutes:150,marks:150,sections:[{id:'language',name:'Language (qualifying)',count:30},{id:'gs',name:'General Studies',count:40},{id:'subject',name:'Computer Science',count:80}],negative:null,cutoff:'Language qualifying ≈ 30%; merit uses Parts II + III'}};
const seed=[{id:crypto.randomUUID(),bank:'STET CS',section:'Computer Science',topic:'Programming & Data Structures',difficulty:'Medium',question:'Which data structure follows the LIFO principle?',options:['Queue','Stack','Tree','Graph'],answer:1,explanation:'A stack follows Last-In, First-Out.'},{id:crypto.randomUUID(),bank:'TRE 4.0 CS',section:'Computer Science',topic:'DBMS',difficulty:'Easy',question:'Which language is primarily used to query relational databases?',options:['HTML','SQL','CSS','XML'],answer:1,explanation:'SQL is the standard language for relational database queries.'}];
function load(){try{return JSON.parse(localStorage.getItem(STORAGE_KEY))||{questions:seed,templates:[],attempts:[]}}catch{return{questions:seed,templates:[],attempts:[]}}}let state=load();function save(){localStorage.setItem(STORAGE_KEY,JSON.stringify(state))}
const app=document.querySelector('#app');
function shell(content,active='dashboard'){app.innerHTML=`<div class="app-shell"><aside><div class="brand"><span class="brand-mark">TE</span><div><b>Test Engine</b><small>V1 · Production</small></div></div><nav>${[['dashboard','⌂','Dashboard'],['bank','▦','Question Bank'],['builder','＋','Test Builder'],['attempts','◷','Attempts']].map(x=>`<button class="nav ${active===x[0]?'active':''}" data-page="${x[0]}"><i>${x[1]}</i>${x[2]}</button>`).join('')}</nav><div class="side-note"><b>Local & Private</b><span>Your data stays in this browser.</span></div></aside><main><header><div><span class="kicker">TEST ENGINE</span><h1>${content.title||'Dashboard'}</h1></div><div class="header-actions"><button class="ghost" id="export">Export Data</button><label class="ghost file-btn">Import Data<input id="import" type="file" accept="application/json" hidden></label></div></header>${content.body}</main></div>`;document.querySelectorAll('.nav').forEach(b=>b.onclick=()=>route(b.dataset.page));document.querySelector('#export').onclick=exportData;document.querySelector('#import').onchange=importData}
function route(page='dashboard'){if(page==='bank')return bank();if(page==='builder')return builder();if(page==='attempts')return attempts();dashboard()}
function dashboard(){shell({title:'Dashboard',body:`<section class="hero"><div><span class="pill">● SYSTEM READY</span><h2>Build. Practice. <em>Improve.</em></h2><p>Create exam-accurate mock tests for Bihar STET and BPSC TRE 4.0 Computer Science, then turn every attempt into actionable insight.</p><div class="hero-actions"><button class="primary" onclick="route('builder')">Create Mock Test →</button><button class="secondary" onclick="route('bank')">Manage Question Bank</button></div></div><div class="hero-orb"><div class="orb-ring"></div><strong>150</strong><span>MAX QUESTIONS</span></div></section><div class="section-head"><div><span class="kicker">PRESETS</span><h3>Start with an exam</h3></div></div><div class="cards">${examCard(exams.stet,'No negative marking')} ${examCard(exams.tre,'Negative marking configurable')}</div><div class="stats"><div><span>QUESTIONS</span><b>${state.questions.length}</b></div><div><span>ATTEMPTS</span><b>${state.attempts.length}</b></div><div><span>TEMPLATES</span><b>${state.templates.length}</b></div><div><span>STORAGE</span><b>LOCAL</b></div></div>`})}
function examCard(e,note){return `<article class="exam-card"><span class="number">${e.short}</span><h3>${e.name}</h3><p>${e.questions} questions · ${e.minutes} minutes · ${note}</p><button class="secondary" onclick="startTest('${e.id}')">Configure & Start →</button></article>`}
function bank(){shell({title:'Question Bank',body:`<div class="toolbar"><div><b>${state.questions.length}</b> questions stored locally</div><div><button class="secondary" onclick="document.querySelector('#csv').click()">Import CSV</button><input id="csv" type="file" accept=".csv,text/csv" hidden></div></div><div class="table-wrap"><table><thead><tr><th>Question</th><th>Bank</th><th>Topic</th><th>Difficulty</th><th></th></tr></thead><tbody>${state.questions.map(q=>`<tr><td>${escapeHtml(q.question)}</td><td>${q.bank}</td><td>${q.topic||'—'}</td><td><span class="tag">${q.difficulty||'—'}</span></td><td><button class="icon-btn" onclick="deleteQuestion('${q.id}')">×</button></td></tr>`).join('')}</tbody></table></div><div class="import-card"><h3>Bulk import</h3><p>Import JSON or CSV question banks. Expected fields: question, options A–D, answer, subject/section, topic, difficulty, explanation, source/year.</p><button class="secondary" onclick="downloadTemplate()">Download CSV Template</button><button class="secondary" onclick="document.querySelector('#jsonimport').click()">Import JSON</button><input id="jsonimport" type="file" accept="application/json" hidden></div>`},'bank');document.querySelector('#csv').onchange=importCsv;document.querySelector('#jsonimport').onchange=importJson}
function builder(){shell({title:'Test Builder',body:`<div class="builder-grid"><section class="panel"><span class="kicker">EXAM PRESET</span><h3>Choose your test</h3><div class="choice-grid">${Object.values(exams).map(e=>`<button class="choice" onclick="selectExam('${e.id}')"><b>${e.short}</b><span>${e.questions} Q · ${e.minutes} min</span></button>`).join('')}<button class="choice" onclick="selectCustom()"><b>Custom Test</b><span>Build your own structure</span></button></div><div id="builder-form"><p class="muted">Select a preset to configure and launch.</p></div></section><section class="panel summary"><span class="kicker">ENGINE</span><h3>Config-driven scoring</h3><div class="rule"><span>Question type</span><b>Single-correct MCQ</b></div><div class="rule"><span>Persistence</span><b>Indexed browser storage</b></div><div class="rule"><span>Navigation</span><b>CBT palette</b></div><div class="rule"><span>Data portability</span><b>JSON export / import</b></div></section></div>`},'builder')}
function selectExam(id){const e=exams[id];document.querySelector('#builder-form').innerHTML=`<div class="config"><div class="config-row"><label>Preset</label><strong>${e.name}</strong></div><div class="config-row"><label>Questions</label><strong>${e.questions}</strong></div><div class="config-row"><label>Duration</label><strong>${e.minutes}:00</strong></div><div class="config-row"><label>Negative marking</label><strong>${e.negative===null?'Configurable':e.negative===0?'None':'Enabled'}</strong></div><div class="config-row"><label>Cutoff reference</label><strong>${e.cutoff}</strong></div><button class="primary full" onclick="startTest('${id}')">Start ${e.short} Mock →</button></div>`}
function selectCustom(){document.querySelector('#builder-form').innerHTML=`<div class="config"><label>Number of questions <input id="custom-count" type="number" min="1" max="150" value="20"></label><label>Time limit (minutes) <input id="custom-time" type="number" min="1" max="300" value="30"></label><label>Negative marks <input id="custom-negative" type="number" step="0.01" max="0" value="0"></label><button class="primary full" onclick="startCustom()">Start Custom Mock →</button></div>`}
function startCustom(){startSession({id:'custom',name:'Custom Mock Test',short:'CUSTOM',questions:+document.querySelector('#custom-count').value,minutes:+document.querySelector('#custom-time').value,marks:+document.querySelector('#custom-count').value,negative:+document.querySelector('#custom-negative').value,sections:[{id:'custom',name:'Custom',count:+document.querySelector('#custom-count').value}]})}
function startTest(id){startSession(exams[id])}let session=null;
function startSession(e){const pool=state.questions.slice();while(pool.length<e.questions){pool.push({id:crypto.randomUUID(),bank:e.short,section:e.sections[0]?.name||'Custom',topic:'Unspecified',question:'Practice question placeholder — import your question bank to populate this test.',options:['Option A','Option B','Option C','Option D'],answer:0,explanation:'This is a placeholder item.'})}session={exam:e,questions:pool.slice(0,e.questions),answers:Array(Math.min(e.questions,pool.length)).fill(null),status:Array(Math.min(e.questions,pool.length)).fill('not'),index:0,start:Date.now(),seconds:e.minutes*60};testScreen()}
function testScreen(){const s=session,q=s.questions[s.index];app.innerHTML=`<div class="test-shell"><header class="test-header"><div class="brand"><span class="brand-mark">TE</span><b>${s.exam.short}</b></div><div class="timer" id="timer">${fmt(s.seconds)}</div><button class="danger" onclick="confirmSubmit()">Submit Test</button></header><div class="test-body"><aside class="palette"><h3>Question Palette</h3><div class="legend"><span>Not Visited</span><span>Answered</span><span>Review</span></div><div class="palette-grid">${s.questions.map((_,i)=>`<button class="pbtn ${s.status[i]}" onclick="jump(${i})">${i+1}</button>`).join('')}</div></aside><section class="question"><div class="q-meta"><span>Question ${s.index+1} of ${s.questions.length}</span><span>${q.topic||q.section||'General'}</span></div><h2>${escapeHtml(q.question)}</h2><div class="options">${q.options.map((o,i)=>`<button class="option ${s.answers[s.index]===i?'selected':''}" onclick="answer(${i})"><span>${String.fromCharCode(65+i)}</span>${escapeHtml(o)}</button>`).join('')}</div><div class="test-controls"><button class="secondary" onclick="clearAnswer()">Clear Response</button><button class="review" onclick="reviewNext()">Mark for Review & Next</button><button class="primary" onclick="nextQ()">Save & Next →</button></div></section></div></div>`;session.interval=setInterval(()=>{s.seconds--;const t=document.querySelector('#timer');if(t)t.textContent=fmt(s.seconds);if(s.seconds<=0){clearInterval(session.interval);finishTest()}},1000)}
function answer(i){session.answers[session.index]=i;session.status[session.index]='answered';testScreen()}function clearAnswer(){session.answers[session.index]=null;session.status[session.index]='not';testScreen()}function reviewNext(){session.status[session.index]=session.answers[session.index]===null?'review':'answered-review';nextQ()}function nextQ(){session.index=Math.min(session.index+1,session.questions.length-1);testScreen()}function jump(i){session.index=i;testScreen()}function confirmSubmit(){if(confirm(`Submit test?\nAnswered: ${session.answers.filter(x=>x!==null).length}\nUnanswered: ${session.answers.filter(x=>x===null).length}`))finishTest()}function finishTest(){clearInterval(session.interval);const e=session.exam;let correct=0,wrong=0;session.questions.forEach((q,i)=>{if(session.answers[i]===q.answer)correct++;else if(session.answers[i]!==null)wrong++});const neg=e.negative===null?0:(e.negative||0);const score=correct+(wrong*neg);const attempt={id:crypto.randomUUID(),exam:e.short,name:e.name,score,correct,wrong,unanswered:e.questions-wrong-correct,total:e.questions,time:e.minutes*60-session.seconds,date:new Date().toISOString()};state.attempts.push(attempt);save();report(attempt)}
function report(a){app.innerHTML=`<div class="report"><header><div><span class="kicker">TEST COMPLETE</span><h1>${a.exam} Analysis</h1></div><button class="primary" onclick="route('dashboard')">Back to Dashboard</button></header><section class="score-grid"><div class="score-card"><span>RAW SCORE</span><b>${a.score.toFixed(2)}</b><small>out of ${a.total}</small></div><div class="score-card"><span>ACCURACY</span><b>${a.correct}/${a.total}</b><small>${a.total?Math.round(a.correct/a.total*100):0}% correct</small></div><div class="score-card"><span>TIME TAKEN</span><b>${fmt(a.time)}</b><small>${a.unanswered} unanswered</small></div><div class="score-card"><span>STATUS</span><b>${a.score/a.total>=.5?'ON TRACK':'NEEDS WORK'}</b><small>Compare with current notification</small></div></section><section class="panel report-panel"><h3>Attempt recorded locally</h3><p>This V1 report is computed entirely in-browser. Detailed section/topic and question-level analytics will use the same attempt data model as the engine expands.</p><button class="secondary" onclick="route('attempts')">View Attempt History</button></section></div>`}
function attempts(){shell({title:'Attempt History',body:`<section class="panel"><h3>Your progress</h3>${state.attempts.length?`<div class="history">${state.attempts.slice().reverse().map(a=>`<div><b>${a.exam}</b><span>${a.correct}/${a.total} correct · ${a.score.toFixed(2)} marks</span><small>${new Date(a.date).toLocaleString()}</small></div>`).join('')}</div>`:'<p class="muted">No attempts yet. Build your first mock test.</p>'}</section>`},'attempts')}
function deleteQuestion(id){state.questions=state.questions.filter(q=>q.id!==id);save();bank()}function exportData(){const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='test-engine-backup.json';a.click();URL.revokeObjectURL(a.href)}function importData(e){const f=e.target.files[0];if(!f)return;f.text().then(t=>{state=JSON.parse(t);save();route('dashboard')}).catch(()=>alert('Invalid backup JSON.'))}function importJson(e){const f=e.target.files[0];if(!f)return;f.text().then(t=>{const d=JSON.parse(t);state.questions=[...state.questions,...(Array.isArray(d)?d:d.questions||[])];save();bank()}).catch(()=>alert('Invalid JSON.'))}function importCsv(e){const f=e.target.files[0];if(!f)return;f.text().then(t=>{const lines=t.trim().split(/\r?\n/),head=lines.shift().split(',');const rows=lines.map(l=>l.split(','));rows.forEach(r=>{const o={};head.forEach((h,i)=>o[h.trim()]=r[i]?.trim()||'');o.options=[o.optionA||'Option A',o.optionB||'Option B',o.optionC||'Option C',o.optionD||'Option D'];o.answer=Math.max(0,(o.answer||'A').toUpperCase().charCodeAt(0)-65);state.questions.push({id:crypto.randomUUID(),bank:o.bank||'Custom',section:o.section||o.subject||'Custom',topic:o.topic||'',difficulty:o.difficulty||'',question:o.question||'',options:o.options,answer:o.answer,explanation:o.explanation||''})});save();bank()}).catch(()=>alert('Could not parse CSV.'))}function downloadTemplate(){const t='question,optionA,optionB,optionC,optionD,answer,bank,section,topic,difficulty,explanation\nExample question?,Option A,Option B,Option C,Option D,A,STET CS,Computer Science,Programming,Medium,Explanation';const b=new Blob([t],{type:'text/csv'}),a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='test-engine-question-template.csv';a.click()}function fmt(sec){sec=Math.max(0,sec);return `${String(Math.floor(sec/60)).padStart(2,'0')}:${String(sec%60).padStart(2,'0')}`}function escapeHtml(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
window.route=route;window.startTest=startTest;window.startCustom=startCustom;window.selectExam=selectExam;window.selectCustom=selectCustom;window.answer=answer;window.clearAnswer=clearAnswer;window.reviewNext=reviewNext;window.nextQ=nextQ;window.jump=jump;window.confirmSubmit=confirmSubmit;window.deleteQuestion=deleteQuestion;route();
