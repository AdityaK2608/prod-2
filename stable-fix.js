const STORE='test-engine-v4';
const app=document.getElementById('app');
let state={questions:[],attempts:[],templates:[]};
try{state=JSON.parse(localStorage.getItem(STORE)||'null')||state}catch(e){}
if(!Array.isArray(state.questions))state.questions=[];
if(!Array.isArray(state.attempts))state.attempts=[];
if(window.BUILTIN_QUESTIONS){const ids=new Set(state.questions.map(q=>q.id));window.BUILTIN_QUESTIONS.forEach(q=>{if(!ids.has(q.id))state.questions.push(q)});try{localStorage.setItem(STORE,JSON.stringify(state))}catch(e){}}
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const save=()=>{try{localStorage.setItem(STORE,JSON.stringify(state))}catch(e){}};
const pct=(a,b)=>b?Math.round(a/b*100):0;
const exams={"STET CS":{name:'Bihar STET Paper II — Computer Science',n:150,m:150,neg:0},"TRE 4.0 CS":{name:'BPSC TRE 4.0 — Computer Science',n:150,m:150,neg:-1/3}};
function shell(title,body,active){app.innerHTML=`<div class="app-shell"><aside><div class="brand"><span class="brand-mark">TE</span><div><b>Test Engine</b><small>V4 · Production</small></div></div><nav><button class="nav ${active==='home'?'active':''}" id="nHome">⌂ Dashboard</button><button class="nav ${active==='practice'?'active':''}" id="nPractice">◉ Practice Lab</button><button class="nav ${active==='builder'?'active':''}" id="nBuilder">＋ Test Builder</button><button class="nav ${active==='attempts'?'active':''}" id="nAttempts">◷ Attempts</button></nav><div class="side-note"><b>Exam Integrity</b><span>Exam interaction controls are restricted.</span></div></aside><main><header><div><span class="kicker">TEST ENGINE</span><h1>${esc(title)}</h1></div><span class="pill">● PRODUCTION</span></header>${body}</main></div>`;nHome.onclick=home;nPractice.onclick=practiceLab;nBuilder.onclick=()=>builder();nAttempts.onclick=attempts}
function home(){const total=state.attempts.length,avg=total?Math.round(state.attempts.reduce((s,a)=>s+pct(a.correct,a.total),0)/total):0;shell('Dashboard',`<section class="hero"><div><span class="pill">● READY FOR PRACTICE</span><h2>Prepare with <em>purpose.</em></h2><p>Premium exam-focused mock testing built for STET and TRE Computer Science preparation.</p><div class="hero-actions"><button class="primary" id="practiceBtn">Open Practice Lab →</button><button class="secondary" id="mockBtn">Start a Mock Test</button></div></div><div class="hero-orb"><strong>${avg||'—'}</strong><span>${avg?'AVG. ACCURACY':'FIRST ATTEMPT'}</span></div></section><div class="section-head"><span class="kicker">PREPARATION TRACKS</span><h3>Choose your exam</h3></div><div class="cards">${Object.entries(exams).map(([bank,e])=>`<article class="exam-card"><span class="number">${bank}</span><h3>${e.name}</h3><p>${e.n} questions · ${e.m} minutes</p><button class="secondary examBtn" data-bank="${bank}">Prepare →</button></article>`).join('')}</div><div class="stats"><div><span>QUESTIONS AVAILABLE</span><b>${state.questions.length}</b></div><div><span>TESTS COMPLETED</span><b>${total}</b></div><div><span>AVG ACCURACY</span><b>${avg?avg+'%':'—'}</b></div><div><span>MODE</span><b>PRIVATE</b></div></div>`,'home');practiceBtn.onclick=practiceLab;mockBtn.onclick=()=>builder();document.querySelectorAll('.examBtn').forEach(b=>b.onclick=()=>builder(b.dataset.bank))}
function practiceLab(){shell('Practice Lab',`<section class="practice-hero"><div><span class="kicker">FOCUSED PRACTICE</span><h2>Practice what matters.</h2><p>Short, focused sessions designed to sharpen Computer Science concepts without committing to a full mock.</p></div><div class="practice-badge"><b>∞</b><span>FLEXIBLE PRACTICE</span></div></section><div class="section-head"><span class="kicker">PRACTICE MODES</span><h3>Choose how you want to practice</h3></div><div class="practice-grid"><article class="practice-card"><span class="practice-icon">10</span><h3>Quick Practice</h3><p>10 mixed questions for a fast daily session.</p><button class="primary pgo" data-count="10" data-name="Quick Practice">Start 10 Questions →</button></article><article class="practice-card"><span class="practice-icon">20</span><h3>Focused Practice</h3><p>20 questions with a balanced difficulty mix.</p><button class="primary pgo" data-count="20" data-name="Focused Practice">Start 20 Questions →</button></article><article class="practice-card"><span class="practice-icon">⚡</span><h3>Challenge Mode</h3><p>Harder Computer Science questions to test your depth.</p><button class="primary pgo" data-count="20" data-name="Challenge Mode" data-hard="1">Start Challenge →</button></article><article class="practice-card"><span class="practice-icon">↻</span><h3>Mixed Revision</h3><p>30 random questions across the available CS topics.</p><button class="primary pgo" data-count="30" data-name="Mixed Revision">Start Revision →</button></article></div><section class="practice-info"><div><b>Practice mode</b><span>Untimed · progress stays in your browser</span></div><div><b>Exam integrity</b><span>Copy, paste, selection and context-menu restrictions remain active.</span></div></section>`,'practice');document.querySelectorAll('.pgo').forEach(b=>b.onclick=()=>practiceStart(+b.dataset.count,b.dataset.name,b.dataset.hard==='1'))}
function practiceStart(count,name,hard){let pool=state.questions.slice();if(hard)pool=pool.filter(q=>String(q.difficulty||'').toLowerCase()==='hard');if(pool.length<count){alert('Not enough questions are available for this practice session.');return}pool.sort(()=>Math.random()-.5);practiceRun({name,qs:pool.slice(0,count),i:0,a:Array(count).fill(null)})}
function practiceRun(s){const q=s.qs[s.i];app.innerHTML=`<div class="test-shell practice-shell"><header class="test-header"><div class="brand"><span class="brand-mark">TE</span><b>PRACTICE LAB</b></div><div class="test-meta"><span>${esc(s.name.toUpperCase())} · QUESTION ${s.i+1} / ${s.qs.length}</span><div class="progress"><i style="width:${pct(s.i+1,s.qs.length)}%"></i></div></div><div class="practice-label">UNTIMED</div><button class="secondary" id="exitP">Exit</button></header><div class="test-body"><aside class="palette"><div class="palette-title"><h3>Questions</h3><span>${s.a.filter(x=>x!==null).length} answered</span></div><div class="palette-grid">${s.qs.map((_,i)=>`<button class="pbtn ${s.a[i]!==null?'answered':''} ${i===s.i?'current':''}" data-q="${i}">${i+1}</button>`).join('')}</div></aside><section class="question"><div class="q-meta"><span class="q-number">PRACTICE ${String(s.i+1).padStart(2,'0')}</span><span>${esc(q.topic||q.section||'Computer Science')}</span></div><div class="question-card"><h2>${esc(q.question)}</h2><div class="options">${q.options.map((o,i)=>`<button class="option ${s.a[s.i]===i?'selected':''}" data-a="${i}"><span class="option-letter">${String.fromCharCode(65+i)}</span><span>${esc(o)}</span><b>${s.a[s.i]===i?'✓':''}</b></button>`).join('')}</div></div><div class="test-controls"><button class="secondary" id="clearP">Clear Response</button><button class="secondary" id="prevP">← Previous</button><div></div><button class="primary" id="nextP">${s.i===s.qs.length-1?'Finish Practice':'Next Question →'}</button></div><div class="autosave">Practice session · Answer selection saved locally</div></section></div></div>`;document.querySelectorAll('[data-q]').forEach(b=>b.onclick=()=>{s.i=+b.dataset.q;practiceRun(s)});document.querySelectorAll('[data-a]').forEach(b=>b.onclick=()=>{s.a[s.i]=+b.dataset.a;practiceRun(s)});clearP.onclick=()=>{s.a[s.i]=null;practiceRun(s)};prevP.onclick=()=>{s.i=Math.max(0,s.i-1);practiceRun(s)};nextP.onclick=()=>s.i===s.qs.length-1?practiceFinish(s):(s.i++,practiceRun(s));exitP.onclick=practiceLab}
function practiceFinish(s){const c=s.qs.reduce((n,q,i)=>n+(s.a[i]===q.answer?1:0),0),w=s.qs.reduce((n,q,i)=>n+(s.a[i]!==null&&s.a[i]!==q.answer?1:0),0);app.innerHTML=`<div class="analysis-page"><header><div><span class="kicker">PRACTICE COMPLETE</span><h1>${esc(s.name)}</h1><p>${s.qs.length} questions completed</p></div><div><button class="secondary" id="backP">← Practice Lab</button><button class="primary" id="againP">Practice Again →</button></div></header><section class="analysis-hero"><div class="analysis-score"><span>YOUR SCORE</span><strong>${c}</strong><small>out of ${s.qs.length}</small></div><div class="analysis-verdict"><span class="pill">● ${pct(c,s.qs.length)}% ACCURACY</span><h2>${c/s.qs.length>=.7?'Good work':'Keep practicing'}</h2><p>Review missed questions and strengthen the underlying concepts.</p></div></section><section class="analysis-panel"><span class="kicker">ANSWER REVIEW</span><h3>${c} correct · ${w} incorrect · ${s.qs.length-c-w} unanswered</h3>${s.qs.map((q,i)=>`<details><summary>Q${i+1} · ${s.a[i]===q.answer?'Correct':s.a[i]===null?'Unanswered':'Incorrect'}</summary><p>${esc(q.question)}</p><p><b>Your answer:</b> ${s.a[i]===null?'—':esc(q.options[s.a[i]])}<br><b>Correct answer:</b> ${esc(q.options[q.answer])}</p><p>${esc(q.explanation||'')}</p></details>`).join('')}</section></div>`;backP.onclick=practiceLab;againP.onclick=()=>practiceStart(s.qs.length,s.name,false)}
function builder(bank){shell('Test Builder',`<section class="builder-grid"><section class="panel"><span class="kicker">SMART TEST GENERATOR</span><h3>Choose an exam</h3><div class="choice-grid"><button class="choice" id="bs"><b>STET CS</b><span>150 Q · 150 min</span></button><button class="choice" id="bt"><b>TRE 4.0 CS</b><span>150 Q · 150 min</span></button></div><div id="config"><p class="muted">Select an exam to review its configuration.</p></div></section><section class="panel"><span class="kicker">PREMIUM ENGINE</span><h3>Built for exam conditions</h3><div class="rule"><span>Selection</span><b>Randomized product bank</b></div><div class="rule"><span>Navigation</span><b>CBT palette + review</b></div><div class="rule"><span>Timing</span><b>Live countdown</b></div><div class="rule"><span>Integrity</span><b>Browser interaction lock</b></div></section></section>`,'builder');bs.onclick=()=>configure('STET CS');bt.onclick=()=>configure('TRE 4.0 CS');if(bank)configure(bank)}
function configure(bank){const e=exams[bank];if(!e)return;config.innerHTML=`<div class="config"><div class="config-row"><label>Exam</label><strong>${esc(e.name)}</strong></div><div class="config-row"><label>Questions</label><strong>${e.n}</strong></div><div class="config-row"><label>Duration</label><strong>${e.m} minutes</strong></div><div class="config-row"><label>Scoring</label><strong>${e.neg===0?'1 mark · no negative marking':'1 mark · −1/3 for wrong'}</strong></div><button class="primary full" id="launch">Start ${esc(bank)} →</button></div>`;launch.onclick=()=>startTest(bank)}
function startTest(bank){const e=exams[bank],pool=state.questions.filter(q=>q.bank===bank);if(pool.length<e.n){alert(`Only ${pool.length} matching questions are loaded; ${e.n} are required.`);return}renderTest({e,bank,qs:pool.sort(()=>Math.random()-.5).slice(0,e.n),i:0,a:Array(e.n).fill(null),review:Array(e.n).fill(false),left:e.m*60,timer:null})}
function fmt(s){return `${String(Math.floor(Math.max(0,s)/60)).padStart(2,'0')}:${String(Math.max(0,s)%60).padStart(2,'0')}`}
function renderTest(s){clearInterval(s.timer);document.body.classList.add('in-exam');const q=s.qs[s.i];app.innerHTML=`<div class="test-shell"><header class="test-header"><div class="brand"><span class="brand-mark">TE</span><b>${esc(s.bank)}</b></div><div class="test-meta"><span>QUESTION ${s.i+1} / ${s.e.n}</span><div class="progress"><i style="width:${pct(s.i+1,s.e.n)}%"></i></div></div><div class="timer" id="timer">${fmt(s.left)}</div><button class="danger" id="submit">Submit Test</button></header><div class="test-body"><aside class="palette"><div class="palette-title"><h3>Questions</h3><span>${s.a.filter(x=>x!==null).length} answered</span></div><div class="legend"><span>Green = Answered</span><span>Yellow = Review</span></div><div class="palette-grid">${s.qs.map((_,i)=>`<button class="pbtn ${s.a[i]!==null?'answered':''} ${s.review[i]?'review':''} ${i===s.i?'current':''}" data-q="${i}">${i+1}</button>`).join('')}</div></aside><section class="question"><div class="q-meta"><span class="q-number">QUESTION ${String(s.i+1).padStart(3,'0')}</span><span>${esc(q.topic||q.section||'Computer Science')}</span></div><div class="question-card"><h2>${esc(q.question)}</h2><div class="options">${q.options.map((o,i)=>`<button class="option ${s.a[s.i]===i?'selected':''}" data-a="${i}"><span class="option-letter">${String.fromCharCode(65+i)}</span><span>${esc(o)}</span><b>${s.a[s.i]===i?'✓':''}</b></button>`).join('')}</div></div><div class="test-controls"><button class="secondary" id="clear">Clear Response</button><button class="review" id="review">${s.review[s.i]?'★':'☆'} Mark for Review</button><div></div><button class="secondary" id="prev">← Previous</button><button class="primary" id="next">${s.i===s.e.n-1?'Submit Test':'Save & Next →'}</button></div><div class="autosave"><span class="save-dot"></span> Response saved locally · Exam integrity active</div></section></div></div>`;document.querySelectorAll('[data-q]').forEach(b=>b.onclick=()=>{s.i=+b.dataset.q;renderTest(s)});document.querySelectorAll('[data-a]').forEach(b=>b.onclick=()=>{s.a[s.i]=+b.dataset.a;renderTest(s)});clear.onclick=()=>{s.a[s.i]=null;renderTest(s)};review.onclick=()=>{s.review[s.i]=!s.review[s.i];renderTest(s)};prev.onclick=()=>{s.i=Math.max(0,s.i-1);renderTest(s)};next.onclick=()=>s.i===s.e.n-1?submitExam(s):(s.i++,renderTest(s));submit.onclick=()=>submitExam(s);s.timer=setInterval(()=>{s.left--;const t=document.getElementById('timer');if(t)t.textContent=fmt(s.left);if(s.left<=300&&t)t.classList.add('warning');if(s.left<=0)finish(s)},1000)}
function submitExam(s){const answered=s.a.filter(x=>x!==null).length;if(confirm(`Submit test now?\nAnswered: ${answered}\nUnanswered: ${s.e.n-answered}`))finish(s)}
function finish(s){clearInterval(s.timer);document.body.classList.remove('in-exam');let c=0,w=0;s.qs.forEach((q,i)=>{if(s.a[i]===q.answer)c++;else if(s.a[i]!==null)w++});state.attempts.push({id:Date.now(),exam:s.bank,score:c+w*s.e.neg,correct:c,wrong:w,unanswered:s.e.n-c-w,total:s.e.n,date:new Date().toISOString()});save();if(typeof window.attempts==='function')window.attempts();else attempts()}
function attempts(){document.body.classList.remove('in-exam');const list=state.attempts.slice().reverse();shell('Attempts',`<section class="panel"><span class="kicker">HISTORY</span><h3>Your recent attempts</h3>${list.length?list.map(a=>`<div class="analysis-history-row"><div><b>${esc(a.exam)}</b><small>${new Date(a.date).toLocaleString()}</small></div><span>${Number(a.score).toFixed(2)} / ${a.total}</span><strong>${pct(a.correct,a.total)}%</strong></div>`).join(''):'<p class="muted">No attempts yet. Start a mock or open Practice Lab.</p>'}`,'attempts')}
home();
